# Frontend Prototype (CP1)

This folder contains the static HTML/CSS/JS frontend used for fast CP1 demo iteration.

Entry points:
- `index.html`
- `login.html`
- `signup.html`

Notes:
- This is a static prototype, separate from the React scaffold under `frontend/src`.
- CP2 can migrate this UI into React once backend integration is stable.
